<div class="nav-bar" x-data>
  <button
    class="nav-item"
    :class="{ 'active': $root.activeTab === 'itinerary' }"
    @click="$root.switchTab('itinerary')"
  >
    <span class="material-symbols-outlined">calendar_month</span>
    <span class="nav-label">Itinerario</span>
  </button>

  <button
    class="nav-item"
    :class="{ 'active': $root.activeTab === 'finances' }"
    @click="$root.switchTab('finances')"
  >
    <span class="material-symbols-outlined">payments</span>
    <span class="nav-label">Finanzas</span>
  </button>

  <button
    class="nav-item"
    :class="{ 'active': $root.activeTab === 'settings' }"
    @click="$root.switchTab('settings')"
  >
    <span class="material-symbols-outlined">settings</span>
    <span class="nav-label">Ajustes</span>
  </button>
</div>
